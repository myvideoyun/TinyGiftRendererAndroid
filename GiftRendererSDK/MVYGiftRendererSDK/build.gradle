apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    buildToolsVersion "28.0.3"

    defaultConfig {
        minSdkVersion 17
        targetSdkVersion 28
        versionCode 1001
        versionName "1.0.01"
    }
}

dependencies {
    implementation project(':libyuv')
    implementation project(':GiftRenderer')
}

